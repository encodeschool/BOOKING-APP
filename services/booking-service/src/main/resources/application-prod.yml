server:
  port: 9084

spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:5432/booking_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
