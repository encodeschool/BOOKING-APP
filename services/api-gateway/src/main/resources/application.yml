spring:
  application:
    name: api-gateway

  logging:
    file:
      name: logs/log.txt

  config:
    import: optional:configserver:http://config-server:8888

jwt:
  secret: mysupersecuresecretkeythatisatleast32characterslong123
