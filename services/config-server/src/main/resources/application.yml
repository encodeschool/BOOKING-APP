spring:
  application:
    name: config-server

  logging:
    file:
      name: logs/log.txt

  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/config

server:
  port: 9888