spring:
  application:
    name: auth-service

  config:
    import:
      - optional:configserver:http://localhost:9888
      - optional:configserver:http://config-server:9888

  profiles:
    active: dev
